import { AppViewContainerDirective } from './app-view-container.directive';

describe('AppViewContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppViewContainerDirective();
    expect(directive).toBeTruthy();
  });
});
